// ‌Component (必须)‌：路由匹配时渲染的 React 组件。
// ‌loader (可选)‌：在渲染前获取数据的函数。
// ‌action (可选)‌：处理表单提交等数据变更的函数。
// ‌ErrorBoundary (可选)‌：当 loader、action 或 Component 抛出错误时显示的组件。
// ‌shouldRevalidate (可选)‌：控制数据重新验证策略的函数。

export { default as Component } from '@/pages/index/index';
